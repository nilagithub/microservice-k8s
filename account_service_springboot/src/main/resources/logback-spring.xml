<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<springProperty scope="context" name="springAppName" source="spring.application.name"/>
	<springProperty scope="context" name="serverName" source="HOSTNAME" />
	<springProperty scope="context" name="LOG_PATH" source="logging.path" />

	<!-- Console Appender -->
            <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
                <encoder>
                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
                </encoder>
            </appender>

            <!-- Rolling File Appender -->
            <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <file>${LOG_PATH}/account_service.log</file>
                <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                    <fileNamePattern>${LOG_PATH}/account_service-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                    <maxFileSize>10MB</maxFileSize>
                    <maxHistory>30</maxHistory>
                    <totalSizeCap>1GB</totalSizeCap>
                </rollingPolicy>
                <encoder>
                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} -- [TraceID: %X{traceId},SpanID: %X{spanId}] %msg%n</pattern>
                </encoder>
            </appender>

            <!-- Root Logger -->
            <root level="INFO">
                <!--<appender-ref ref="CONSOLE"/>-->
                <appender-ref ref="FILE"/>
            </root>

            <!-- Specific Logger Levels (Example for Spring Cloud Gateway) -->
            <!--<logger name="org.springframework.cloud.gateway" level="INFO"/>
            <logger name="reactor.netty" level="INFO"/>-->

</configuration>